trigger:
  branches:
    include:
      - dev
pool:
  name: 'Default'

steps:
- checkout: self
- script: |
    cd iac/azure/env/dev
    terraform init
  displayName: 'Terraform Init'

- script: |
    cd iac/azure/env/dev
    terraform plan
  displayName: 'Terraform Plan'
